# MRT PHARMA — AUTHORITATIVE BIM SPACE GEOMETRY + TRUE ROOM-LOCAL ANCHOR DIAGNOSTIC

Diagnostic-first spatial-authority build. The previous fixed-anchoring build
FAILED manual acceptance: the range-derived rectangle does not convincingly
identify one architectural room. This build does NOT adjust the rectangle. It
adds a read-only spatial-authority probe + a pure classifier so the ACTUAL
authority the BIM provides for `1AC1 CENTRAL WAITING` can be established live,
before any overlay change.

```
CURRENT_FIXED_LOCATION_ACCEPTANCE = FAIL
ROOM_OVERLAY_CHANGED_BEFORE_AUTHORITY_DIAGNOSIS = NO
NEXT_ROOM_OVERLAY_IMPLEMENTATION = WAIT_FOR_LIVE_AUTHORITY_DIAGNOSTIC
```

---

## 1. RECONCILE

```
PRECHECK_HEAD        = b2abe6bf28056d002d140b52fb54e94c9cd417eb
PRECHECK_ORIGIN_MAIN = b2abe6bf28056d002d140b52fb54e94c9cd417eb
PRECHECK_DIVERGENCE  = 0 0 (origin/main...HEAD)
EXISTING_UNCOMMITTED_WORK_PRESERVED = YES
```

Nothing staged/committed/reset/reverted/pushed. `frontend/.env` and
`AS_IS_DIGITAL_TWIN_PHASE_1A_SEAM_REPORT.md` untouched.

---

## 2. WHAT WAS BUILT (diagnostic only)

| Piece | File |
|---|---|
| Pure authority classifier `classifyRoomSpatialAuthority(...)` with explicit precedence + `overlaySourceForAuthority` | `frontend/src/components/spatial/roomSpatialAuthority.ts` (new) |
| Read-only live probe `probeRoomSpatialAuthority(...)` — EC class + hierarchy, geometry stream / placement / calculated range / spatial-index range, IFC space representation, spatial hierarchy, physical containment + boundary relationships (walls/doors), IfcRelSpaceBoundary, adjacency, derivation feasibility | `frontend/src/components/spatial/roomSpatialAuthorityProbe.ts` (new) |
| Overlay entrypoint `diagnoseRoomSpatialAuthority()` — targets the persisted assignment by bimSpaceId | `spatialAssetOverlay.ts` |
| `DIAGNOSE UPTAKE 01 SPATIAL AUTHORITY` button (proven pointer pattern, synchronous `ROOM_SPATIAL_DIAGNOSTIC_START_RECEIVED` before async) | `AuditDiagnosticsPanel.tsx` |
| Offline tests (13) | `frontend/src/tests/roomSpatialAuthority.test.ts` |

Authority precedence (strict): `EXACT_SPACE_GEOMETRY` > `EXPLICIT_SPACE_BOUNDARY_RELATIONSHIPS`
> `DERIVABLE_FROM_PHYSICAL_BOUNDARIES` > `RANGE_ONLY_APPROXIMATION` > `ANCHOR_ONLY`
> `NO_USABLE_SPATIAL_AUTHORITY`. A range NEVER promotes to an exact/boundary class
(`SPATIAL_INDEX_RANGE_COUNTS_AS_EXACT_BOUNDARY = NO`).

Identity authority is `bimSpaceId` — no name/nearest/screen search
(`ROOM_DIAGNOSTIC_IDENTITY_AUTHORITY = BIM_SPACE_ID`,
`ROOM_LOCATION_SCREEN_SPACE_DERIVATION = NO`, `NEAREST_GEOMETRY_ROOM_GUESS = NO`).

---

## 3. VERIFICATION

```
TYPECHECK              = PASS
OFFLINE_TEST_COUNT     = 527 (27 files)
NEW_TEST_COUNT         = 13 (roomSpatialAuthority.test.ts)
OFFLINE_TEST_REGRESSIONS = 0  (baseline 514 preserved)
PRODUCTION_BUILD       = PASS
CORE_FRONTEND_VERSION  = 5.12.5
WORKER_ASSET_REAL      = YES  (head = (()=>{"use strict";f)
DRACO_WASM_ASSET_REAL  = YES  (magic = 0061 736d)
DEV_SERVER             = clean restart, http://localhost:3000/viewer → HTTP 200
BENTLEY_WRITE_API_CALL_COUNT = 0 · IMODEL_MODIFIED = NO · BENTLEY_CHANGESET_CREATED = NO
```

Offline authority tests: RANGE_ONLY_AUTHORITY, EXACT_SPACE_AUTHORITY,
EXPLICIT_BOUNDARY_AUTHORITY, DERIVABLE_BOUNDARY_AUTHORITY, NO_SPATIAL_AUTHORITY,
precedence (multiple sources), range-not-promoted, overlay-source mapping — all PASS.

---

## 4. §62 REQUIRED FIELDS

```
PRECHECK_HEAD = b2abe6bf28056d002d140b52fb54e94c9cd417eb
PRECHECK_ORIGIN_MAIN = b2abe6bf28056d002d140b52fb54e94c9cd417eb
PRECHECK_DIVERGENCE = 0 0
EXISTING_UNCOMMITTED_WORK_PRESERVED = YES
CURRENT_FIXED_LOCATION_ACCEPTANCE = FAIL
CLINICAL_PROGRAM_DECORATOR_REGRESSION = 0
PROGRAM_ASSIGNMENT_REGRESSION = 0
BIM_RANGE_AS_EXACT_ROOM_BOUNDARY = REJECTED
BIM_RANGE_ROLE = COARSE_SPATIAL_METADATA_ONLY
AUTHORITATIVE_ROOM_GEOMETRY_DIAGNOSTIC = IMPLEMENTED
ROOM_SPATIAL_DIAGNOSTIC_TRIGGER = PROVEN_DIAGNOSTICS_PANEL
ROOM_DIAGNOSTIC_IDENTITY_AUTHORITY = BIM_SPACE_ID
TARGET_SPACE_IDENTITY = REPORTED (live)
TARGET_SPACE_CLASS_HIERARCHY = REPORTED (live)
SPACE_GEOMETRY_STREAM = REPORTED_LIVE
SPACE_PLACEMENT = REPORTED_LIVE
SPACE_CALCULATED_RANGE = REPORTED_LIVE
SPACE_SPATIAL_INDEX_RANGE = REPORTED_LIVE
SPATIAL_INDEX_RANGE_COUNTS_AS_EXACT_BOUNDARY = NO
IFC_SPACE_REPRESENTATION_SEARCH = PERFORMED
IFC_SPACE_REPRESENTATION_FOUND = REPORTED_LIVE
AUTHORITATIVE_SPACE_BOUNDARY_FOUND = REPORTED_LIVE
TARGET_SPACE_SPATIAL_HIERARCHY = REPORTED (live)
SPACE_PHYSICAL_CONTAINMENT_SEARCH = PERFORMED
SPACE_BOUNDARY_RELATIONSHIP_SEARCH = PERFORMED
TARGET_SPACE_BOUNDARY_ELEMENT_COUNTS = REPORTED (live)
TARGET_SPACE_DOOR_RELATIONSHIP = REPORTED_LIVE
TARGET_SPACE_ADJACENCY_SOURCE = REPORTED_LIVE
TARGET_SPACE_ADJACENT_SPACE_COUNT = REPORTED_LIVE
PHYSICAL_BOUNDARY_DERIVATION_FEASIBLE = REPORTED_LIVE
ROOM_LOCATION_SCREEN_SPACE_DERIVATION = NO
NEAREST_GEOMETRY_ROOM_GUESS = NO
ROOM_SPATIAL_AUTHORITY_CLASSIFIER_TESTABLE_OFFLINE = YES
ROOM_SPATIAL_AUTHORITY_PRECEDENCE = EXPLICIT
RANGE_ONLY_AUTHORITY_TEST = PASS
EXACT_SPACE_AUTHORITY_TEST = PASS
EXPLICIT_BOUNDARY_AUTHORITY_TEST = PASS
DERIVABLE_BOUNDARY_AUTHORITY_TEST = PASS
NO_SPATIAL_AUTHORITY_TEST = PASS
ROOM_SPATIAL_AUTHORITY_PRECEDENCE_TEST = PASS
ROOM_OVERLAY_CHANGED_BEFORE_AUTHORITY_DIAGNOSIS = NO
UPTAKE_01_OVERLAY_SOURCE = UNCHANGED_PENDING_LIVE_AUTHORITY
EXACT_ROOM_EXTENT_AVAILABLE = PENDING_LIVE_DIAGNOSTIC
UPTAKE_01_ROOM_LOCAL_ANCHOR = PENDING_LIVE_AUTHORITY
ROOM_LOCAL_ANCHOR_COORDINATE_SYSTEM = BIM_WORLD
ROOM_BOUNDARY_COORDINATE_SYSTEM = BIM_WORLD (when authority supports it)
ROOM_LOCAL_SPATIAL_STATE_CAMERA_DEPENDENCE = NONE
CLINICAL_PROGRAM_DIAGNOSTIC_REGRESSION = 0
BENTLEY_ROOM_RENAMED = NO
BENTLEY_ROOM_GEOMETRY_MODIFIED = NO
BENTLEY_WRITE_API_CALL_COUNT = 0
IMODEL_MODIFIED = NO
BENTLEY_CHANGESET_CREATED = NO
NAVIGATION_ARCHITECTURE_CHANGED = NO
EQUIPMENT_PIPELINE_CHANGED = NO
ACTIVE_BIM_PERSISTENCE_CHANGED = NO
PROJECT_BIM_SELECTOR_REGRESSION = 0
ADDITIONAL_DEMO_ROOM_ASSIGNMENTS = 0
TRANSPORT_MODE_IMPLEMENTATION_STARTED = NO
PATIENT_ANIMATION_IMPLEMENTED = NO
CARRIER_ANIMATION_IMPLEMENTED = NO
SIMULATION_CHANGED = NO
ECONOMIC_MODEL_CHANGED = NO
TYPECHECK = PASS
OFFLINE_TEST_COUNT = 527
NEW_TEST_COUNT = 13
OFFLINE_TEST_REGRESSIONS = 0
PRODUCTION_BUILD = PASS
CORE_FRONTEND_VERSION = 5.12.5
WORKER_ASSET_REAL = YES
DRACO_WASM_ASSET_REAL = YES
DEV_SERVER = PASS
VIEWER_HTTP_STATUS = 200
CHECKPOINT = HOLD_FOR_MANUAL_DIAGNOSTIC
MANUAL_UPTAKE_01_EXACT_ROOM_BOUNDARY = NOT_ACCEPTED
MANUAL_UPTAKE_01_FIXED_PHYSICAL_ROOM = NOT_ACCEPTED
CLINICAL_PROGRAM_SPATIAL_ACCEPTANCE = HOLD
```

The `REPORTED_LIVE` values require one manual click of the diagnostic to capture
against the live iModel — the probe emits every §41 field.

---

## 5. MANUAL DIAGNOSTIC (next action)

Developer mode ON → BIM AUDIT DIAGNOSTICS → **DIAGNOSE UPTAKE 01 SPATIAL AUTHORITY**
(one click). It runs against the persisted `bimSpaceId` and prints EC class, class
hierarchy, geometry stream/placement/ranges, IFC representation, boundary element
counts, adjacency, derivation feasibility, and the final
`ROOM_SPATIAL_AUTHORITY_CLASS`.

Decision gate: the next correction's overlay source is chosen from that live class
(exact geometry → authoritative polygon; explicit boundaries → derive from them;
derivable → deterministic physical polygon; range-only → fixed anchor/marker, stop
the rectangle masquerading as a room). No overlay change until then.

STOP. Do not stage/commit/push.
