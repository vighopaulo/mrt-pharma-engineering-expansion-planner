# MRT Pharma™ Digital Twin — Clean Build

Deploy `app.py` from the repository root. Keep `model.py`, `requirements.txt`, and the `.streamlit` folder beside it.

This build separates the optimization engine from the Streamlit interface and includes a tested default-scenario smoke test.
